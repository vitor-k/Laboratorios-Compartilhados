<%= form_with(model: [@laboratorio, equipamento], local: true) do |form| %>
  <% if equipamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equipamento.errors.count, "error") %> prohibited this equipamento from being saved:</h2>

      <ul>
      <% equipamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :nome, "Nome" %>
    <%= form.text_field :nome, class: 'form-control' %>
  </div>

<div class="form-group">
    <%= form.label :funcao, "Função" %>
    <%= form.text_field :funcao, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :taxa, "Taxa diária" %>
    <%= form.number_field :taxa, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
  <br/>
<% end %>
