<h1>Novo Pedido</h1>

<% if (@user != nil) %>
    <%= render 'form', pedido: @pedido %>
<% else %>
    <div id="error_explanation">
        <h2> Deve estar logado para pedir </h2>
    </div>
<% end %>

<%= link_to 'Voltar', pedidos_path %>
