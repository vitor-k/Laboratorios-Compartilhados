<h1>Editing Serviço</h1>

<% if (admin_signed_in? || @user == @responsavel) %>
    <%= render 'form', servico: @servico %>
<% else %>
    <div id="error_explanation">
        <h2> Você não tem permissão para editar o serviço </h2>
    </div>
<% end %>


<%= link_to 'Show', [@laboratorio, @servico] %> |
<%= link_to 'Página de servicos', laboratorio_servicos_path(@laboratorio) %>
